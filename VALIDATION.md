# 0.4.4 验证记录

日期：2026-09-24。分析用户第二份日志（本地 Laya，506 次决策，对局以 battle_ended 结束）后：探图 205 次被问、203 次选等待，海军 100 次被问多选巡逻，结束时来不及读胜负。

- `npm test`：58 项通过。新增：战损账本（首次观测只登记不计数、新单位计新建、视野内消失的敌人计击毁、迷雾中消失不计并在超时后遗忘）、连续三次拒绝探图后自动出发一次、能打到威胁的舰船编入保卫任务、未战败结束标记为 ended、结果标记只允许弹窗且校验取值、战绩曲线带兵力与战损字段。
- 预览页确认「趋势」与战绩详情各新增两张曲线（每张 3 条线），结果标记按钮可切换、列表同步更新并提示，清除后恢复。
- 未在真实对局验证自动探索与海军追击，需要用户实测。

---

# 0.4.3 验证记录

日期：2026-09-24。分析用户导出的战役日志（本地 Laya，386 次决策）后修三处：后台标签页定时器节流导致决策每分钟一次（日志中 tick 21692–64892 期间决策严格每 3600 tick 一次）、海军不自卫、缺少对局总结。

- `npm test`：54 项通过。新增：游戏拍子唤醒决策循环（定时器失效时每个新拍子恰好问一次、同拍不重复、停止后注销回调且不再提问）、无 `onTick` 时退回定时器并在开始事件中标注、潜艇在射程内自动回击且不被通道清理移动、战绩记录（计数、结果、资金曲线、按时间倒序、重复停止不重复记录、监视不产生记录、导出不含密钥、清空）。
- 预览页确认「战绩」标签：列表（时间、胜负、时长、来源、回答数、结束资金）、详情三项指标、事实行、经济与决策曲线、导出与清空按钮。
- 未在真实对局验证后台节流修复；需用户切走标签页后观察决策计数是否持续增长。

---

# 0.4.2 验证记录

日期：2026-09-24。出击门槛改为按实际生产能力与部队增长判断；探图不再因见过敌方建筑而停止；新增「本局目标」设置。

- `npm test`：50 项通过。新增战役场景 4 项：有车厂时仍按 8 车 + 防空规则（7 车无护卫不出击、补齐后出击，期间探图继续）；无车厂时 8 名武装步兵即出击（留 1 名侦察兵）、1 名时继续训练并侦察；完全不能生产时有 2 个单位就出击；部队 2700 tick 未增长即出击、刚增长则重置计时；见过敌方哨塔后探图仍提供；目标文本进入战术说明与 state.objective。后台 1 项：目标保存时压缩空白、超 300 字拒绝、开启托管时传给页面、内容脚本拿不到。
- 既有遭遇战测试全部保持通过（含「基地防空不算随军护卫」「8 车才进攻」等）。
- 预览页确认设置页新增目标输入框、超长时行内提示。
- 未在真实战役中验证效果；需要用户实测并导出决策日志。

---

# 0.4.1 验证记录

日期：2026-09-24。新增决策日志（扩展本机记录 + 导出 + 汇总）、本地服务请求日志和离线分析脚本。

- `npm test`：45 项通过。新增日志模块 4 项（记录裁剪与脱敏、事件归类、条数 / 体积上限、统计口径）和后台 1 项（问答 / 动作 / 失败入库、导出仅限弹窗、导出不含密钥与令牌、后台重启后仍可读、清空）。
- 实际排查：本地 Laya 服务日志显示扩展已发来 551 次请求全部 200，确认托管走的是本地模型而非官方 Jev。用真实规模请求测得 Laya 在 construction / defenses / vehicles / infantry 组几乎总选 wait（0.72–0.99），只有 tactics 组选进攻，这是「有回答但不造东西」的直接原因。
- 预览页确认：设置页底部显示日志汇总（记录数、决策数、失败数、平均响应、动作受理 / 等待 / 未执行、各组等待率）、导出触发下载并提示文件名、清空后显示空状态；保存过「本地 Laya」后重新打开设置页直接选中本地来源。
- 第二个本地服务实例加 `--log` 跑真实规模请求，JSON Lines 每行含候选、选择、概率、耗时；`tools/analyze-log.mjs` 对该文件和导出 JSON 都能输出汇总。

---

# 0.4.0 验证记录

日期：2026-09-23。新增「模型来源」开关（Jev 云端 / 本地 Laya）和本地 Laya 决策服务；未修改游戏引擎、玩家 API 或策略。

## 自动检查与发布包

- `npm test`：39 项通过，0 失败。新增 3 项：两种来源各自保存地址与密钥、本地来源无需密钥且不发送 Authorization 头、切换来源或修改当前来源配置停止托管、清除本地令牌不影响 Jev 密钥、连接测试跟随当前来源、Jev 来源仍要求密钥、本地服务 HTTP 503 记为失败但不停止托管、局域网私有地址允许 HTTP 而公网地址仍拒绝。
- `dist/page.js` 不再包含任何模型服务地址（默认值改为可摇树的纯对象），打包检查同时校验 `api.typesafe.ai` 与 `127.0.0.1:8742` 都不出现在游戏页脚本中。
- `npm run package` 成功。包路径：`artifacts/werhd-jev-extension-0.4.0.zip`。SHA-256：`23926519255a090f6b1befa421fed4943ce4d24f5f187097f25f58713c229158`。

## 本地 Laya 服务

- `npm run laya` 用 laya-vs-jev 的 Python 3.14 / MLX 环境加载 `laya-multilingual-mlx`（0.8–1.1 秒），监听 `127.0.0.1:8742`。
- 用扩展「测试模型连接」的原样请求体调用 `/v1/systemone`，返回 `choice: ok`，26 ms。用 5 组候选、7 KB 战况的真实规模请求连续 5 次，平均往返 181 ms，5 个回答全部通过扩展的候选校验（`validateAnswer`）；60 个候选的极端请求仍返回 200。错误路径：错误路径返回 404、缺少 questions 返回 422、非 JSON 返回 422，服务不中断。
- 用假模型对服务单元检查：状态字段重排只改顺序不丢字段；缺 state、空 questions、非法类型均被拒；设置令牌后无令牌或错误令牌返回 401，正确令牌返回 200；超过 256 KB 的请求体返回 413 且不触达模型；`/health` 报告令牌开关与请求计数。
- `--lan` 模式监听 `0.0.0.0`，启动时打印 `http://10.0.25.215:8742/v1` 与访问令牌，令牌持久保存在 `~/.laya-server-token`（0600）。从局域网地址无令牌调用返回 401，带令牌调用返回正确回答。

## 界面

- 密钥与本地令牌输入框右侧新增「显示 / 隐藏」按钮，切换后输入框在明文 / 密文之间变化，标题随语言切换（显示密钥 / Show key）。
- 「测试模型连接」按钮自身显示三种状态：测试中（禁用 + 转圈 + 「正在测试 Jev 连接…」）、成功（绿色「✓ Jev 连接成功 · 123 ms · jev-1.13」）、失败（琥珀色「✗ Jev 连接失败，…」）；结果不再写入上方提示条。修改任一设置或切换来源后按钮复位为「测试模型连接」；未保存时点击仍提示先保存。以上在预览页中逐一点击并截图确认。
- 地址、密钥、模型名在保存时和后台读取时都去掉首尾空格，新增测试覆盖带空格输入。
- 打开设置时已保存的 JEV 密钥 / 本地令牌直接填入输入框，默认以密码圆点遮住，点小眼睛才明文；保存后重新填入并恢复遮住。表单里的内容就是保存的内容：清空本地令牌并保存即清除；Jev 来源清空密钥保存会行内提示。更换 Jev 服务地址时若密钥未变会拒绝保存并提示重新输入。密钥只发给扩展自己的弹窗，内容脚本、状态接口、悬浮窗仍拿不到（测试覆盖）。预览页确认：加载即显示遮住的密钥、小眼睛切换明文、改密钥保存后回填并遮住。
- 表单校验改为行内提示：保存或测试时，每个有问题的输入框红框并在框下方显示原因（密钥为空、地址非法、模型名非法、快捷键非法、上限超范围），一次显示全部问题，焦点落到第一个；「更多设置」里的字段出错会自动展开。修改该字段后提示立即消失，切换来源清空全部。后台返回的密钥 / 地址类错误也映射到对应输入框，其余错误仍走顶部提示条。预览页用空密钥 + 公网 HTTP 地址 + 空上限三处同时出错验证，截图确认样式与中英文文案。
- `node tools/preview.mjs` 预览：设置页顶部显示「模型来源」分段开关，切到「本地 Laya」后只显示本地服务地址、访问令牌（可选）和启动提示，Jev 密钥 / API 地址 / .env 导入隐藏；「更多设置」按来源切换 Jev 模型名 / 本地模型名；按钮文案改为「测试模型连接」。中英文均已切换检查。

## 验证边界

- 本次没有在真实 Chrome 中加载 0.4.0 扩展并开局：自动化环境无法加载未打包扩展（品牌版 Chrome 已移除 `--load-extension`，Chrome for Testing 在本会话中启动卡住）。真实浏览器中的保存授权、测试连接和对局托管由用户手动验证。
- 本地模型受 1024 token 上下文限制，战况被截断，决策质量未评估，不代表胜率。

---

# 0.3.0 验证记录

日期：2026-09-23。仅修改独立扩展，未修改游戏引擎、公开玩家 API 或线上站点。

## 自动检查与发布包

- `npm test`：36 项通过，0 失败。新增 7 项覆盖展示条件、停止 / 隐藏后的迟到响应、标签页可见性、连接失效、拖动边界、文档隔离、设置持久化与状态通知。
- 展示条件为 `running && showOverlay`。停止、设置关闭、页面隐藏或销毁后，正在等待的旧状态响应不能重新挂载窗口。重新允许展示必须先确认当前运行状态。
- 新增内容脚本状态接口只返回同一标签页、文档和来源的显示字段；不返回密钥、会话令牌、原始事件或 API 地址。读取期间只检查现有控制器状态，不注入玩家、不调用模型、不提交游戏指令。
- 设置开关即时保存，后台重启后保留；切换开关不会创建或停止托管会话。模型调用、游戏命令与既有策略测试保持通过。
- `npm run package` 成功；ZIP 的 14 个条目完整性校验通过。包路径：`artifacts/werhd-jev-extension-0.3.0.zip`。SHA-256：`8eacea5a6d7e7c29632ef3a51f37a8bd6163da5ae46d4f026e24777549c418b6`。

## 真实 Chrome / 线上游戏验证

- 已安装扩展更新到 0.3.0，ID 不变，保留原有密钥。测试地址为 `https://ra2web.github.io/`，游戏版本 0.87.0；正常菜单进入「湖泊之战 (2)」，利比亚，对手 AI-简单，资金 10000、初始部队 0、速度 6。
- 主菜单和进入对局后、未开启托管时，均无悬浮状态窗。按 `Alt+Shift+J` 后出现中文状态窗，初始 tick 518、资金 10000、回答 0；随后读取真实回答 2、资金 9569、响应 337 ms。
- 点击收起后仅保留标题状态条。在收起按钮有焦点时再按同一快捷键，出现停止提示，状态窗立即从页面移除。弹窗确认该会话回答数 3、停止按钮禁用。
- 在设置中关闭展示并切换英文，关闭后重新打开弹窗仍为未勾选。此时通过工具栏按钮开启托管，游戏页无状态窗；弹窗确认托管运行、回答 1、资金 6447，说明隐藏展示没有阻止托管。
- 同一运行会话重新允许展示，立即出现英文状态条；展开后显示 `JEV · wannafire`、`Jev is in control` 及真实指标。标题拖动后截图确认窗口从左侧移至画面内侧。
- 使用工具栏停止按钮停止第二段托管，回答数 37；恢复中文后关闭弹窗，页面没有状态窗。随后快捷键重新开启，正常使用「放弃任务 → 退出」离开对局；结算时状态窗自动移除，扩展显示「对局已结束」、回答 2、tick 8611，停止按钮禁用。
- 收尾停留中文、允许展示开启、托管停止。关闭了本次临时测试标签页，保留用户原有标签页。

## 验证边界

- 本次验证悬浮状态窗及控制状态同步，不评估策略胜率。测试期间多次手动开启 / 停止，最后主动退出产生失败结算，不作为完整策略对战结果。
- 自然战败、HTTP 错误和请求预算停止沿用既有后台停止流程；自动测试覆盖停止通知，新增窗口实测覆盖手动快捷键、停止按钮和主动退出。全屏、Edge、窄屏及长期挂机未做本轮人工验收。

---

# 0.2.1 官网入口

日期：2026-09-23。弹窗底部与使用说明新增随语言切换的官网入口：中文「王二火大」→ https://wangerhuoda.com/；英文「wannafire」→ https://wannafire.com/。使用新标签页打开。

- `npm test`：既有 29 项通过；构建、打包与 ZIP 完整性检查通过。
- 已将原 Chrome 扩展重新加载为 0.2.1。在真实扩展弹窗及帮助页分别切换中文/英文，核对显示名称与链接目标同步变化。
- 收尾恢复中文，未开启托管。本次仅新增界面链接，没有修改游戏 API 或策略；没有重新开展对局测试。

---

# 0.2.0 验证记录

日期：2026-09-23。只修改 `werhd-jev-extension`，没有修改游戏、游戏 API 或线上站点。

## 自动化与界面

- `npm test`：29 项通过，0 失败。保留 0.1.1 的 22 项回归；新增只读观测不产生模型请求/命令、跨后台重启保存采样、识别新局、语言切换保留凭据及运行状态、零值/未知值/可见样本边界、300 点容量与采样间隔、时钟回退、空图/负值/常量曲线、中英文文案完整性。
- 内置本地预览以 410×600 弹窗尺寸检查中英文战场、趋势和设置。两张折线图共 3 条曲线、鼠标定位采样、资金双线和图例可见；DOM 测得可用宽度与滚动宽度均为 395px（扣除滚动条），无横向溢出。空对局有明确等待采样状态。预览数据只用于排版验证，不作为实战证据。
- 中文/English 持久化切换，英文名称为 WannaFire；帮助页支持两种语言。单位名称使用游戏提供的标签，英文界面使用规则代号。

## 已安装 Chrome 与线上实战

- 在原扩展 ID `gdlbfdhmjlbgokjjnocibejeaagnphpi` 上重新加载 `dist/`，Chrome 显示 0.2.0。权限范围不变。已保存密钥、API `https://api.typesafe.ai/v1`、快捷键 Alt+Shift+J 和镜头选项均保留。
- 主菜单打开弹窗不自动托管；中英文切换立即生效。关闭再打开弹窗仍为英文。
- 从 `https://ra2web.github.io/` 正常菜单进入「城市突击战 (2)」，己方美国、对手 AI-简单随机、资金 10000、初始部队 0、速度 6。
- **未开启托管时**，弹窗 tick 718、资金 10000、模型回答 0、响应耗时为空，态势为 LIVE / read-only，读到自身基地位置、电力和单位状态。只读监控调用模型次数为零的机制由后台自动化测试覆盖。
- 17:18 左右手动开启，真实 Jev 返回有效回答：回答数由 0 到 2、3、4；响应时间观察到 1132、362、298ms。累计回答曲线和真实资金/队列剩余投入双线随战况更新。过程中切换中文，计数保持 3、托管仍在运行。
- tick 3300 点击停止时累计 4 次模型回答、资金 6783。随后 tick 3868、4318、4678 均维持 4 次回答，同时资金和实时态势继续刷新；这是只读观察和已有生产继续运行，不是重新托管。
- 停止后真实态势读到视野内敌军 1、基地附近敌军 1、采矿单位 1、电力 200/50、矿场队列 100% 待放置。模型控制已停止，监控没有代为放置建筑。
- 通过游戏正常菜单「放弃任务 → 退出」结束本次短局。重新打开弹窗显示「最后观测 · 非实时」，保留 tick 5308、4 次回答、资金 6450 的最后快照；没有自动继续控制。

- 发布包 `artifacts/werhd-jev-extension-0.2.0.zip` 的 14 个条目完整性通过，实际本地密钥扫描无命中，不含开发预览、测试、依赖或配置文件。SHA-256：`dda756c541d3f62cf7d6af9d65c995c7d9a076ea0930fb19e37eec3f82ad04dc`。

- 收尾重新加载最终打包版本并刷新游戏页，默认停留中文、未托管状态；本地模拟预览已关闭。
- 三个游戏 API / 镜头文件 SHA-256 与 0.1.1 验证记录中的基线一致。

## 边界

- 本次新增界面和数据链路已用真实 Chrome / 线上游戏 / Jev 验证。射程告警布局使用模拟压力样本验证；其计算沿用已有射程分析回归，短实测没有等到光棱压制场景。
- 图表是当前会话的有界采样，不是跨浏览器重启保存的完整战报；每次开启托管重置计数，新局清空旧图。资金、电力和血量未知值不伪装成零，结束后的快照标明非实时。
- 不属于胜率或策略强度验收。其他地图、阵营、Edge 和长期运行仍未完成验证。

---

# 0.1.1 验证记录

日期：2026-09-23。本项目只实现独立浏览器扩展；本次未改动游戏引擎、玩家 API 或线上站点。

## 已完成

- `npm test`：22 项通过，0 失败。包含既有的策略、射程防守、部署、兵种和通道、特殊任务、镜头测试，以及执行器的过期战况、停止、自然结束保护。
- 扩展后台测试：使用模拟 Chrome API 和模拟 Jev 响应，验证密钥仅供可信扩展页面管理、请求地址来自设置、候选校验、标签页/文档/会话隔离、停止后拒绝迟到回答、并发保护、后台重启不重置预算、预算耗尽停止、HTTP 402 立即停止、更换凭据停止旧会话、页面导航撤销会话。
- 页面消息测试：实际页面适配器与编译后的 content script 在隔离测试上下文中收发消息；游戏策略替换为测试桩。验证请求/响应、取消、重复注入、会话替换和对局身份切换。这不等同于 Chrome 真实执行上下文验证。
- 界面人工操作：在浏览器以 390×600 尺寸打开 `tools/preview.mjs` 的模拟预览，验证密钥字段、保存、录入 Ctrl+Shift+K、开启/停止后的按钮与状态切换、纵向滚动；修复了窄窗口横向滚动。预览不连接模型或真实游戏。
- `npm run package` 成功；`unzip -t artifacts/werhd-jev-extension-0.1.1.zip` 全部条目校验通过。包内仅包含编译脚本、弹窗/帮助页、manifest 和图标，不含测试桩、源码依赖、代理服务或任何密钥。
- 游戏 API 文件的 SHA-256 与本次工作开始时一致：`PlayerConsoleApi.ts`、`PlayerConsoleTypes.ts`、`PlayerConsoleCamera.ts`。

## Chrome 与真实 Jev 已观察到的结果

- 用户确认安装后，通过 Chrome「加载未打包的扩展程序」加载 `dist/`，扩展 ID 为 `gdlbfdhmjlbgokjjnocibejeaagnphpi`，页面显示扩展已加载且启用；真实工具栏弹窗正常连接后台。
- 通过扩展新增的本地文件导入入口，读取此前 `.env.local` 中的三个 Jev 配置字段，并在弹窗保存。密钥没有输出到对话或日志；其他配置未导入。
- 真实「测试 Jev 连接」请求成功，弹窗显示 `937 ms`。本次服务已返回有效候选回答，不沿用历史 HTTP 402 作为当前结论。
- 在游戏主菜单点击开启，明确提示「请先进入一场正在运行的对局」，没有开始游戏控制。
- 在线地址为 `https://ra2web.github.io/`，游戏界面版本 `0.87.0`；通过正常菜单开始「城市突击战 (2)」，己方美国，对手 AI-简单、随机阵营，资金 10000、初始部队 0、速度 6。
- 16:48:24 开启后，日志出现 `deploy_base`、`GAPOWR` 和建筑放置；游戏画面确认移动基地变成建造场、电厂实际出现，资金由 10000 减少。
- 16:49 左右，模型计数先为 7，随后到 15；显示真实响应 363–596 ms、前沿侦察和兵种生产。后续日志显示 `defend_base`、多个单位的「基地接敌」命令。
- 首次停止显示停止状态、15 次决策；随后发现另一次会话重新开启且计数重置。电脑操作工具两次提示用户正在操作同一 Chrome 窗口，因此这段受干扰的样本不计入停止稳定性通过证据。

## 闭环完成结果

1. 第一局自然战败：游戏结算显示「失败」，地图「城市突击战 (2)」，游戏时间 `0:14:13`。己方美国：摧毁 3、损失 25、建造 13、分数 55；对手伊拉克 AI-简单：摧毁 19、损失 3、建造 34、分数 515。扩展先自动停止，记录 `outcome` 和 `defeated_or_observer`，最终会话计数 19。这局有开局后延迟接管和中途开关操作，不能作为公平的策略强度对比。
2. 用户交还 Chrome 后，按同样设置开第二局短流程测试。旧会话未自动接管新局；手动按 `Alt+Shift+J` 出现「Jev 托管已开启」，弹窗显示新对局 tick 691、2 次模型决策和真实资金。
3. 点击「停止」后：tick 1136、决策 3、资金快照 8938、响应 938 ms。等待 12 秒（超过 8 秒后台请求超时），重新完整读取弹窗，仍为相同计数和快照，「停止」按钮禁用，无自动重新开启。
4. 关闭弹窗后，`Alt+Shift+J` 分别验证重新开启和停止，游戏页提示与动作一致。
5. 再次开启后，通过游戏菜单「放弃任务 → 退出」结束短测试。扩展自动显示「对局已结束」，停止按钮禁用。本段会话计数 0，没有把退出后回答计入有效决策。
6. 实际安装、配置保存、直连 Jev、MAIN/ISOLATED 消息桥、正常玩家 API 命令效果、关闭弹窗继续运行、手动停止、快捷键、自然战败和退出清理形成闭环。没有修改游戏代码或线上 API。
7. 收尾已在 Chrome 重新加载 `0.1.1`。真实弹窗显示该版本、已保存密钥、默认 API 和快捷键均保留；刷新后的游戏页处于未托管状态，没有自动发起新会话。

## 尚未完成

- 未验证其他阵营/地图组合、困难 AI、长期挂机稳定性或获胜能力。本次接入链路闭环通过，不是策略强度/胜率验收。
- Edge、其他操作系统及浏览器重启后的人工验证。

## 用户安装后验收顺序

1. 扩展管理页加载 `dist/`，确认无加载错误；在弹窗保存密钥和默认 API 地址。
2. 打开在线游戏并正常进入对局。首次真实请求若返回 401/402/403，应看到明确状态并停止，检查服务/账户后再手动开启。
3. 开启后确认决策计数和战况更新，并观察实际建造、单位命令及可选镜头移动。
4. 按已配置快捷键停止，确认新的模型请求与命令停止；再次开启可继续当前局。
5. 退出或结束对局，确认不会自动接管下一局。

尚未完成的项目不得表述为已经通过。
