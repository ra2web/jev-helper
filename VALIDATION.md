# 0.7.5 验证记录

日期：2026-09-25。依据 0.7.4 实战报告 jev-report-20260925-221915（本地 Laya，选择题，目标「使用工程师占领盟军作战实验室」，第 2144 秒开始托管，结果战败）。

- 过程：推进选项 `objective_1470` 执行 45 次；第 51489 拍「目标附近已无守军」，三名护送工程师进入；第 52615 拍占领完成，GATECH 进入我方建筑。第 52674 拍基地受攻击、我方单位 9→14（含 4 辆犀牛、1 架飞机）；第 52710 拍 GATECH 不在我方建筑中；第 52746 拍多出 NATECH、第二个 NACNST、NAPOWR 2→5；第 52790 拍游戏报告我方 defeated。
- 结论：0.7.4 的推进、护送和「集火不打目标」生效，占领成功；前两局实验室在约 32 分半消失不是时限（本局第 35 分钟时实验室仍在），更可能是 0.7.4 修掉的集火误伤。占领后的战败原因从日志无法判断（实验室被毁还是被剧情转手、是否另有目标）。
- 本版只补记录：目标占领后继续跟踪（每轮血量；离开我方后记 `afterCapture`：changed_hands / gone / out_of_sight 与最后血量）；结局事件记下各势力 defeated、目标最后状态、我方建筑数；日志字段同步放行。
- 测试：新增 1 项（占领后血量、转手与消失两种记录、日志与结局事件字段）。`npm test` 177 项全部通过。
- 版本号 0.7.5。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.7.4 验证记录

日期：2026-09-25。依据 0.7.3 实战报告 jev-report-20260925-213824（本地 Laya，选择题，目标「使用工程师占领盟军作战实验室」，战败）。

- 现象：第 468 秒起认出作战实验室 #1470；工程师组 36 次提问，占领实验室被执行约 15 次、训练工程师约 15 次，全部是单个工程师从约 117 格外出发，没有一次占领完成。作战题多数选「集结」（4/12 未就绪），部队没有向实验室推进。实验室在第 1946–1948 秒消失，第 29249 拍判负；上一局（0.7.2）在第 1969–1971 秒消失、随后判负——两局打法不同但时间几乎相同，判断是任务时限（约 32 分半）。
- 修复：
  - 占领任务的作战题：`objective_<id>` 改为「向目标旁推进」（边走边打到目标 4 格外、我方来的方向，不指定目标、不打实验室），有 6 个作战单位（`CAPTURE_PUSH_UNITS`）或部队就绪即可，模型两次不选自动执行；可推进时不再给「集结」。
  - 有守军的占领目标：工程师任务改为护送（`escort`）：接令时不出发，维持环节每 60 拍把工程师移到推进部队中心后方 4 格；部队中心到目标 8 格内、或目标 8 格内已无武装敌人时下达占领，转为普通占领任务。没有推进部队时工程师原地等待。
  - 集火 / 舰船自卫不再选择要占领或要保护的建筑（整局模拟时发现停在实验室旁的坦克会自动打它）。
  - 有进行中特殊任务的单位不算空闲，护送中的工程师不会被另派去占别的建筑（模拟时发现）。
- 模拟：用 7 辆坦克 + 1 个工程师、模型始终答「等待」跑完整托管循环：第 60 拍自动推进，工程师跟随，第 480 拍部队到实验室旁后工程师进入，全程没有对实验室下攻击。
- 测试：`test/capture-objective.test.mjs` 新增 4 项（6 个单位自动推进、推进点在目标旁且不打目标、4 个单位不自动；护送跟随与到位后占领；守军清完直接占领；集火不打占领目标）。其中「集火不打占领目标」在去掉修复时会失败，已确认。原「有守军时拒绝三次再自动派」改为「有守军时护送、拒绝一次自动」。`npm test` 176 项全部通过。
- 版本号 0.7.4。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.7.3 验证记录

日期：2026-09-25。依据 0.7.2 实战报告 jev-report-20260925-211203（本地 Laya，选择题，新关卡，目标「使用工程师占领盟军战略实验室」，结果战败）。托管从游戏第 493 秒开始。

- 目标：「占领」被归为保护类，目标状态整局是「没找到」；盟军作战实验室（GATECH）第一条决策起就在视野里；工程师组整局只被问到 2 次，都是去占别的建筑。实验室以 `assault_1470` 出现在进攻选项里（第 14776 拍被选中一次）。第 29459 拍实验室还在，第 29568 拍消失，第 29589 拍战败；当时部队在进攻约 11 格外的敌方建造厂。
- 生产：第 12000 拍之后，车辆组给出「犀牛坦克」，模型 251 次选择等待；资金从约 4,200 涨到 14,000，部队从 19 降到 6。
- 修复：
  - 目标解析新增「占领」类（占领 / 夺取 / 俘获 / 攻占 / 拿下 / capture / seize / take over），与「保护」分开；占领目标同时列入「不可攻击」。只写了占领目标时按占领任务追踪；被我方占下记为完成，被摧毁记为 `lost`（占领失败）。
  - 选择题：占领目标不出现 `objective_` / `assault_` 攻击选项，作战题写明「CAPTURE target … never attack it」，守卫它的碉堡排在进攻选项最前；「保护 / 占领」的建筑一律不进进攻选项。工程师组把占领目标排第一，即使有守卫（有守卫 auto 3，无守卫 auto 1）；没有工程师时训练一个（不受 1,800 资金门槛，auto 1），有守卫时多备一个。
  - 指挥模式：占领目标进入合法占领列表（即使有守卫），不在合法攻击列表；系统提示补一句占领目标的规则。
  - 资金 ≥ 5,000 且部队不足 40 个单位时，车辆 / 步兵组里第一个作战单位（非矿车、非工程师）标 auto 2：模型连续两次不造就自动造。
- 核对：用这份报告里出现过的 32 种建筑名匹配，只有 Allied Battle Lab 被认作占领目标和不可攻击建筑，油井等不受影响。
- 测试：新增 `test/capture-objective.test.mjs` 7 项（中英文占领解析与混合分句；实验室不进攻击选项、守卫碉堡排前、工程师第一且有 / 无守卫的自动级别；钱少也训练工程师；占下 / 被毁两种结局；保护的建筑不被进攻；指挥模式合法列表；钱多自动造作战单位、低于门槛不自动）。`npm test` 172 项全部通过。
- 版本号 0.7.3。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.7.2 验证记录

日期：2026-09-25。依据 0.7.1 实战报告 jev-report-20260925-181506（本地 Laya，选择题，目标「摧毁五角大厦」）与 jev-report-20260925-195421（另一关，未设目标）。

- 181506：游戏第 90 秒认出「RA2 Wash Pent B」，之后 A、D、C 依次成为目标，第 864 秒四块全倒、目标完成，随后对局切换（battle_changed）。问题在第一块用了约 10 分钟：这关造不了战车（纯步兵），步兵上限 16，动员兵稳定在 20–23 人，整局只训练了 20 个；资金从约 4,300 涨到 8,900 没花；期间持续被碉堡打、损失 137 个单位。第 763 秒任务增援 16 辆天启坦克后，剩下三块 30 秒内倒下。
- 195421：能造战车的另一关，约 18 分钟打完，损失 31、击杀 93，资金没有堆积，不涉及这次修改。
- 修复：纯步兵部队资金 ≥ 3,000 时步兵上限提到 40（`RICH_INFANTRY_CAP`），资金 ≥ 5,000 且未到上限时补兵选项自动执行；资金不足 3,000 仍按 16 人上限。
- 测试：新增一项（20 名步兵 + 9,000 资金时给出并自动补兵；3,500 时给出但不自动；1,500 时仍受 16 人上限；40 人时即使有钱也不再补）。原有一项「正常门槛下不强制训练」原先用 25,000 资金，改为 3,500 资金验证（25,000 时按新规则应当强制）。`npm test` 165 项全部通过。
- 版本号 0.7.2。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.7.1 验证记录

日期：2026-09-25。依据 0.7.0 实战报告 jev-report-20260925-174055（本地 Laya，选择题模式，目标「摧毁五角大厦」）：整局目标状态都是「没找到」，五角大楼只倒了一半。

- 原因：日志里视野内建筑的名字显示，这张地图上的五角大楼是四座建筑「RA2 Wash Pent A / B / C / D」（CAWA2A–D），没有一座的名字含「Pentagon」，目标匹配一直落空；C、D 两块是在打附近敌人时顺带打掉的，A、B 留到结束。
- 修复：「五角大楼 / 五角大厦」的关键词加上「wash pent」；用英文写「Pentagon」也会带上这个别名（同一组名字里任意一个被提到，整组都算）。目标由几块组成时，打掉一块后按离上一块最近的顺序接着找下一块，全部倒下才算完成。
- 核对：用这份报告里出现过的 38 种建筑名逐个匹配，只有 Wash Pent A–D 四块被认作目标，Wash Building、旗子、路灯等都不算。
- 测试：新增一项（四块五角大楼在中英三种写法下都能认出、普通华盛顿民房不算、「摧毁五角大楼，保护白宫」仍只打五角大楼；按四轮依次给出剩下的一块，四块都没了目标才算完成）；原有关键词断言相应加上「wash pent」。`npm test` 164 项全部通过。
- 版本号 0.7.1。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.7.0 验证记录

日期：2026-09-25。依据 docs/commander-design.md 第二步：把第一步的态势简报与指挥请求接入托管流程。

- 设置：新增「决策方式」（选择题 / 指挥官），只在「OpenAI 兼容」来源生效，Jev、本地 Laya 一律按选择题；修改它与改模型一样会停止正在进行的托管；公开设置、会话、战绩记录都带上决策方式。
- 后台：指挥模式开局时告诉页面 `commander: true`，页面等 95 秒、请求本身 90 秒；指挥请求校验简报为对象且不超过 200,000 字符，非 OpenAI 来源直接拒绝；沿用现有的超时、拒绝重定向、HTTP 错误带服务原因、`tool_choice` 被拒改 auto 并记住、看不懂的文字改 JSON 重发（按 issue_orders 的参数形状）；每轮写一条 `command` 日志（拍、模型、耗时、Token、简报字符数、总体说明、各条命令与理由、被拒原因），不含密钥。
- 页面 / 玩家：新增指挥循环（首轮立即；之后距上次 ≥180 拍且上一请求已返回；基地刚受攻击、小队本轮折损过半、进攻目标被摧毁、首次看到敌方建筑或本局目标时立即；请求进行中不重发）；意图执行与维持（attack 看不见目标时按记忆坐标边走边打、attack_move / move / hold / retreat / scout / defend_base / garrison；维持时只给新加入或空闲的成员补令；进楼只派能进楼的步兵且不超过空位，其余原地停下，靠近敌方防御记为攻坚、碉堡没了自动撤出）；生产（兵按数量入队、钱不够或队列满说明原因；建筑由扩展选址）；工程师占领 / 修桥；基地受攻击 450 拍无回防时最近的小队自动回防并写进下一轮简报；小队合并 / 消失时意图随之转移 / 清除；本能层（还手 / 后撤、集火、舰船自卫、撤楼）照常，撤退和回防的小队不被本能层拉走，移动 / 撤退途中不停下交火；不使用选择题的候选、自动兜底、任务锁、出击门槛。
- 界面：设置页 OpenAI 字段区加「决策方式」下拉与说明（中英）；弹窗「最近执行记录」显示指挥事件；数据面板关键事件时间线显示每轮指挥（总体说明 + 命令与执行结果），决策记录表显示模型的命令与理由，单场详情显示决策方式，日志统计显示指挥轮数、平均耗时、被拒命令数、执行 / 未执行数、自动回防次数。用模拟数据在浏览器里打开弹窗和数据面板检查过显示。
- 测试：新增 `test/commander.test.mjs` 17 项（简报卡片 / 小队编号稳定 / 合法清单 / 碉堡旁民房 / 血量字段 / 中立建筑筛选；schema 枚举、逐条校验与拒绝原因、JSON 形状与包装；后台端点 / 90 秒 / 日志不含密钥 / 战绩与弹窗事件、auto 重发与 JSON 重发、非 OpenAI 拒绝与设置切换停托管；日志体积上限与统计；决策节奏与触发、折损过半 / 目标被毁、进楼人数与其余不冲、记忆坐标与只补新来 / 空闲、各意图执行、自动回防、生产与工程师及失败原因、上一轮结果、小队消失与分出）。旧测试未改动。`npm test` 150 项全部通过。另用仿 0.6.0 实战的场景把完整循环跑了 800 拍：首轮 S1 进 1638 民房（5 人）、S2 回防、补 5 个动员兵、工程师修桥，之后按 180 拍节奏继续，没有异常。
- 复查修正（同日，第二个提交，依据检查 agent 的复现脚本 c1–c7）：
  - 小队合并吞掉意图（c2）：守家小队和路过的进攻纵队相距 8 格内会被合成一队，纵队被拉回守点。现在分组时只有意图相同的单位才会合成一队，没有意图的新兵并入 8 格内最近的小队并接手它的意图。
  - 进楼小队留在外面的成员被本能层派去冲碉堡（c4）：进楼命令本身正确，但下一次微操里 13 人被「8 人以上一起强攻」规则派去打 1655，之后在 1655 / 1656 之间来回。现在进楼小队的成员对固定防御只会后撤到射程外，不会强攻。
  - 坐标不校验、执行出错会停托管：简报的合法清单带上地图尺寸，越界坐标被拒（「coordinates outside the map」）；维持意图时出错只丢弃这条意图并记一条错误，模型回答执行出错也只记错误，都不停托管、也不计入连续失败次数。
  - 被拒命令字段不限长：被拒项的小队 / 动作 / 代号截断到 40 字；「上一轮结果」最多 20 条命令、10 条被拒、总长不超过 8000 字符，不会把下一份简报撑到超限被拒。
  - 紧急重问加最小间隔：除「基地刚被攻击」外，紧急触发距上次决策至少 90 拍。c5（目标每 45 拍死一个）从一分钟 20 次降到 10 次。
  - 自动回防与模型拉锯（c7）：选的做法是按「一次受攻击」计时：从第一次看到威胁开始，直到 300 拍内都没有威胁才算结束，入侵者在边界进进出出仍算同一次，自动回防按这次受攻击开始后累计 450 拍触发，每次只触发一次；自动回防的小队在这次受攻击结束前锁定，模型给它的其他命令记为「auto_defense_active」、不执行（模型下 defend_base 则照常接手）；「上一轮结果」写明「基地仍受攻击，自动回防中」。受攻击结束后锁定解除，模型命令照常生效。
  - 过期回答：回答对应的局面已过去 900 拍以上时，只执行生产和工程师命令，小队意图不执行（「stale_reply」），「上一轮结果」写明「回答过期，小队意图未执行」。
  - hold：到点后被带离超过 4 格且空闲的单位会补令回守点。
  - 进攻敌方部队：选的做法是两个都做：简报里每个敌军群最多给 3 个单位编号（离我方最近小队最近的一个，再按最大血量取最强的），可直接用 attack 指向；系统提示同时说明「要打整群就对它的位置用 attack_move」。
  - 测试：新增 `test/commander-review.test.mjs` 9 项，每项对应一条（P0-1 合并与新兵并入；P1-1 连续微操 40 轮里留守成员不打碉堡、只后撤；P1-2 越界坐标被拒、微操里抛错的意图被丢弃且托管继续；P1-3 超长字段截断与报告上限；P2-1 紧急最小间隔、基地被攻击仍立即；P2-2 进出的入侵者只触发一次自动回防、锁定期内模型命令被拒、结束后恢复、报告写明；P2-3 过期回答；P2-4 hold 漂移补令；P2-5 敌军群编号与提示）。测试用的模拟环境抽到 `test/commander-world.mjs` 供两个测试文件共用。`npm test` 159 项全部通过。
- 二次复查修正（第三个提交，复现脚本 d1、d3b、d3c、d5、d5b）：敌方防御建筑停在基地附近不再算「基地受攻击」（交给模型当敌方建筑处理），自动回防只打移动的入侵者、对防御建筑只后撤；自动回防锁定最长 600 拍，到期交还模型并写进「上一轮结果」，锁定期间的说明告诉模型「现在下 defend_base，或等到第 N 拍」；受攻击状态只在维持环节更新，决策轮先看到第二波攻击时也会再次自动回防；「意图相同」统一为同动作、同目标、坐标 5 格内，重复下达时更新坐标，小队不再碎成十几支（d5b：40 轮后 1 支）；回答过期要同时满足超过 900 拍和实际等待超过 60 秒，游戏加速时正常回答不再被判过期。回防小队不再整体跳过本能层（改为只禁止强攻固定防御），相应改写一条旧断言。新增 4 项测试，`npm test` 163 项全部通过。
- 版本号 0.7.0。未在真实对局中验证（指挥模式需要真实对局里的 gpt / DeepSeek 实测节奏、Token 用量和执行效果）。仅本地提交，未推送、未发版。

---

# 0.6.1 验证记录

日期：2026-09-25。依据用户战报 jev-report-20260925-063240（0.6.0、本地 Laya、目标「摧毁五角大楼」）。

- 数据定位：第 756–948 秒损失从 55 升到 147；这段时间战术多次选同一目标（assault_1600 / 1599），但因部队名单每轮都变，「同一任务」判断不成立，进攻令被重复执行二十多次；射程外受击回应共 16 次，其中多次是「N 个单位抵近还击 Pill Box」；自家门口民房 #1633 进驻 65 次，「空闲乘员重新进入目标」497 次，完成仅 8 次。
- 测试：新增「重复同一目标只发给新来 / 空闲单位、全部在忙时不重发」（决策循环驱动），「被碉堡逼退后该碉堡的据楼攻坚排第一、一轮兜底」，「进不去的楼 3 次后放弃」；按新规则改写「两个动员兵冲碉堡」为「后撤并标记攻坚、8 人才强攻」、「空闲步兵多也给家门口守楼」为「只在基地受攻击时给」；测试模拟接口改为「下令后单位不再空闲」以符合游戏。`npm test` 132 项全部通过。
- 本局目标「摧毁五角大楼」全场未匹配（题面始终是「Target not found yet」，无 objective_ 选项），日志中也没有建筑名称可查；新增：目标未匹配时 state.objectiveTarget.seen 与决策日志 state.objective.seen 记录视野内敌方 / 中立建筑的「名称/代号」（最多 30 个），新增 1 项测试。`npm test` 133 项全部通过。
- 版本号 0.6.1。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.6.0 验证记录

日期：2026-09-24。依据两份 0.5.9 本地 Laya 战报（jev-report-20260924-230426、231135）。

- 数据定位：230426 已推进到敌方基地但五角大楼未被摧毁，部队被碉堡等射程更远的防御打而不还手，损失 162。231135 为同局延续，本局目标「摧毁五角大楼」，但作战选项里从未出现五角大楼：进攻名单只按建造厂排序取 2 座，另 3 个防御位被爱国者导弹（只对空）占满，本局目标只是题面开头的一句话、中文名也对不上英文标签 Pentagon；模型约每 36 拍换一次选择，整队随之掉头，且每次受理都重置任务计时，「进攻停滞」永远不触发；最后 53 次回家集结。另见：防御建造队列里的哨戒炮让「能造战车」为真，出击门槛显示「0/12 辆战车」；自动补兵仍按固定 8 人；Laya 每组只看约 256 Token，进攻失利说明把「部队已就绪」挤出可见范围；无威胁时防御组每轮都答等待，约占 26–32% Token；「尚无已知损坏」时也频繁派工程师修桥。
- 改动：新增 `src/player/werhd-jev-objective.mjs`（本局目标中英对照与建筑匹配、记忆坐标、摧毁判定、「保护 / 占领」不进攻）；`werhd-jev-player.mjs` 中作战组首项 `objective_<id>`（带 auto 2，基地受威胁时排在 defend_base 后且不自动）、进攻名单（只列能打地面的防御，其余按建造厂 > 工厂 > 矿场 > 电厂排序，最多 6 个）、`respondToThreats`（射程外受击时抵近还击或后撤，60 拍冷却）、`forceReadiness`（只认车辆 / 步兵队列、75% 缓冲、失败后停滞等待缩短）、自动补兵按当前门槛、`acceptMission` / `missionGate`（同目标不重置计时，450 拍任务锁）、`stableRallySite`（集结点固定 1800 拍）、engage_visible 限 15 格并写坐标、题面把目标和就绪放最前；`werhd-jev-strategy.mjs` 无威胁时不出防御组（顺带移除只在和平时提供的围墙选项）；`werhd-jev-special.mjs` 修桥只在进攻卡住或可见受损时提供。
- 新增 `test/player-objective.test.mjs` 12 项：「摧毁五角大楼」对上 Pentagon 且排第一、auto 2、爱国者不进名单、碉堡在、进攻 ≤6；兵少也给目标、基地受威胁时先守家；英文目标匹配、进迷雾后用记忆坐标攻击移动、被摧毁后选项消失；「保护 / 占领」不进攻、目标未见时继续探图；哨戒炮不算能造战车；失败升级后 8/12 自动补兵；就绪后在门槛上下 1 波动不翻转、跌破 75% 才转未就绪；任务锁与计时（同目标不重置、换目标重置、守家 / 目标 / 撤退可打断、目标被毁或 450 拍后解锁）；决策循环里交替两个进攻目标只执行第一次、本局目标两次等待后自动执行；射程外受击抵近还击、冷却内不重复、步兵被坦克打后撤、已接入微操循环；engage_visible 15 格限制与坐标；无威胁不出防御组。
- 按新规则改写的旧测试：`player-bridge`「无已知损坏时提供修桥」改为不提供；`player-special` 的桥改为受损状态；`player-range-defense` 两处迎击场景把部队放到敌人 15 格内；`player-roles-traffic` 和平时的围墙选项改为「不问防御组、围墙选址仍保通道」；`player-strategy` 一处对防御组的否定断言改为可选链。
- 用 Laya 分词器按 256 Token 截断重算作战题：模型能看到本局目标、目标坐标、「Force READY」和完整的 `objective_900` 选项。`npm test` 121 项全部通过。
- 复查修正（同日，第二个提交）：
  - 目标选项原先在任何兵力下都带自动执行，且「选集结」也算拒绝，复现为 2 名步兵每两轮被派去打五角大楼、失败升级后 7/16 人照样被送上去、集结一直被锁；另外「已在进攻」只要 ≥4 人就绕过门槛。改为只有就绪（或停滞规则成立）才自动，选集结不算拒绝，已在打同一目标时不重派也不续锁；「已在进攻」需兵力 ≥ 门槛 75%。
  - 敌方只剩爱国者时没有进攻选项：防空塔改为不占防御名额、排在普通建筑末尾。
  - 进攻失利放弃任务时同时清除任务锁；目标 10 格内能打地面的防御不受锁限制。
  - 受击回应性能：每个敌人的对地射程只算一次，按直线距离粗筛，每轮最多检查 32 个单位、每个单位最多看最近 6 个敌人；150 对 150（相距很远）时这一步从约 40 ms 降到约 1.5 ms，整个微操回到改动前水平（约 21 ms）；近距离混战时单轮射程查询有上限。
  - 目标解析按分句处理，保护 / 占领分句里的建筑剔除；英文按整词匹配；目标被我方占领记为已占领。
  - 有本局目标时进攻选项降到 4 个，作战题等待说明缩成一句；还击 / 后撤播报每轮合并、每 150 拍最多一条；后撤的单位 450 拍内不被任务派回；`player-strategy` 中防御组不存在时必然通过的断言改为直接断言「无威胁时不问防御组」。
  - `test/player-objective.test.mjs` 新增 7 项（兵少不自动 / 选集结不锁不派 / 不重派不续锁、75% 才算已在进攻、只剩防空塔仍可打、失利清锁与目标附近碉堡不受锁、分句 / 整词 / 占领、有目标时 4 个进攻选项与短等待说明、大战时有界的查询次数与合并播报与后撤不被派回），原受击测试按合并后的播报格式更新。Laya 分词器按 256 Token 重算：有目标、失败升级、最多选项三种题面下目标、就绪信息与目标选项均完整可见。`npm test` 128 项全部通过。
- 二次复查（第三个提交）：目标分句加入「并 / 并且 / 同时 / 但 / 但是 / but / while」，「不要 / 别 / 不许 / do not / never」等否定分句视为保护，「摧毁五角大楼并保护白宫」「摧毁五角大楼但不要打白宫」「destroy the Pentagon but do not attack the White House」都只打五角大楼，「摧毁五角大楼和白宫」仍是两个目标；有本局目标时，目标 10 格内的地面防御按离目标远近排序，紧贴目标的碉堡一定列出。新增 1 项、改写 1 项断言，`npm test` 129 项全部通过。
- 版本号 0.6.0。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.5.9 验证记录

日期：2026-09-24。依据两份 0.5.8 战报（221719 Jev、221416 本地 Laya）。

- 221719：游戏 816 s 至 4682 s 部队恒为 8（4 动员兵 + 2 工程师 + 2 矿车），无新损失、无生产，资金 17,850 → 72,000；日志中最后 443 个回合战术 / 驻守 / 工程全选等待；出击说明恒为「only 3 units are free to attack; gather at least 4」；题目中无步兵组。原因：无战车工厂可建，步兵仍受「≤6 人、每角色 ≤3」的辅助上限，出击门槛 8 永远达不到，再叠加 0.5.7 的「少于 4 人不进攻」。1876 次决策共 1413 万 Token。
- 221416：两次据楼攻坚目标都是 Patriot Missile（仅对空）；进攻部分集结 141 次。
- 新增测试：无车辆生产时步兵不受辅助上限且资金充足自动造兵、有车辆时保持上限（player-siege 2 项，含对空防御不作攻坚目标与死锁自动攻坚）；连续全等待后放慢提问、恢复后回到原节奏（player-loop-naval 1 项）；角色测试场景补上原本应有的坦克生产。`npm test` 109 项全部通过。
- 版本号 0.5.9。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.5.8 验证记录

日期：2026-09-24。按用户反馈：频繁进出建筑浪费兵力，希望清理完目标后自动撤出。

- 进楼动作带上原因（据楼攻坚 / 前沿 / 家门口）和攻坚目标，受理后记入 memory.garrisons；每次维护检查：楼已不是我方或没人即遗忘；10 格内有武装敌人、或攻坚目标仍在，就重置计时；安静达到 150 / 900 / 2700 拍后下达撤出，记录撤出时间，2 分钟（1800 拍）内不再给该楼的进楼选项。
- `test/player-siege.test.mjs` 新增 3 项：攻坚楼在碉堡还在、附近有步兵、刚清空时都不撤，安静 150 拍后撤出并记录与播报；前沿先于家门口撤出，失效记录被遗忘；刚撤出的楼冷却期内不给选项、冷却后恢复。`npm test` 106 项全部通过。
- 版本号 0.5.8。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.5.7 验证记录

日期：2026-09-24。依据用户四份战报（174003 本地 Laya、174343 Jev、174755 DeepSeek、175118 DeepSeek）。

- 数据定位：进楼选项只看前 10 栋可驻守建筑，几乎都在自家基地旁（同一栋 #1634 被进 6 次），「前沿据点」只按「靠近第一个可见敌方建筑」选，与碉堡无关；进攻选项只列前两个敌方建筑，另一侧碉堡从未出现；Jev 那局「用 1 个单位进攻建造厂」下令 134 次（部队停滞规则允许有多少打多少）；历史提示只认「期间 0 击杀」，添油时总有零星击杀，几乎不触发；DeepSeek 5 次「返回的内容无法解析」触发连续错误停止；175118 首个请求超时后用户手动停止。
- 新增 `test/player-siege.test.mjs` 6 项（每个碉堡一个据楼选项且两侧都有、进攻失利时优先并自动、家门口守楼的给出条件、进攻选项含各侧防御且兵少不进攻、损失多于击杀算没进展、连续 6 次撤下且保卫基地不撤），`test/openai.test.mjs` 新增 1 项（文字回答改 JSON 重发一次，二次失败才计失败）；原「停滞 2 辆坦克照样进攻」的测试按新规则改为 2 辆不攻、4 辆才攻；特殊能力测试的手工局面补上「基地附近有敌人」。`npm test` 103 项全部通过。
- 真实数据重放（Jev 那局 4 个连续进攻建造厂的回合，原题 vs 加历史提示）：gpt-5.6 原题 4/4 继续打建造厂，加提示后 4/4 换打电厂并说明「该进攻已无进展」；本地 Laya 加提示后反而 4/4 选中被标记的选项，另一局中 5 个带 STALE 标记的回合也全选被标记项——因此对 Laya 以撤选项兜底。据楼攻坚需要民房与碉堡坐标，日志里没有，无法重放，由单元测试覆盖。
- 版本号 0.5.7。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.5.6 验证记录

日期：2026-09-24。依据用户三份战报（jev-report-20260924-162304 Jev、163232 本地 Laya、170949 DeepSeek）。

- 数据定位：第 1、3 局修桥选项每次都给了，但被 4 个「占领」选项淹没——目标全是「RA2 Stack O Pipes」「Invisible Yellow Light Post」这类装饰物，工程师每 150 拍「重新前往」一次无限循环；部队的主要进攻目标 `assault_1622` 是「RA2 US Flag Left」（第 1 局下令 157 次、第 3 局 38 次）。第 2 局 Laya 对「重建精炼厂（SURVIVAL）」连续 480 次选等待，没有收入、没有工程师，工程组从未出现。
- 新增 `test/player-bridge.test.mjs` 7 项：装饰物判定（旗 / 路灯 / 管道是装饰，电厂、五角大楼不是；油井可占领、民房不可）、工程师不再收到装饰物占领选项、敌方旗帜不是进攻目标也不进已知建筑、进攻卡住后修桥变为优先并带自动兜底且排在占领前（重复下令不重置计时）、可见受损桥段也触发优先、无工程师时训练工程师写明修桥并可自动、占领被拒 3 次后放弃且不再提供；另在策略测试里确认生存性重建带自动兜底。`npm test` 全部通过。
- 真实数据重放（旧题目 = 日志原题；新题目 = 同一回合按新规则改写），Codex 代理站 gpt-5.6 与本机 Laya：第 1、3 局共 13 个有工程组的回合，旧题目 gpt-5.6 选修桥 0/13（全去占领装饰物，进攻全选国旗）、Laya 10/13；新题目 gpt-5.6 13/13、Laya 13/13，进攻改为迎击可见敌军。第 2 局的等待无法用日志重放复现（日志只有战况摘要和截断的题面，真实请求远长于 Laya 约 1024 Token 的上下文），由自动兜底保证。
- 代理站 gpt-5.6 当天单次 3–36 s，有超过 30 s 上限的情况。
- 版本号 0.5.6。未在真实对局中验证。仅本地提交，未推送、未发版。

---

# 0.5.5 验证记录

日期：2026-09-24。DeepSeek 兼容与地址斜杠修正。

- DeepSeek（用户实测报「Thinking mode does not support this tool_choice」）：两种强制写法都被拒，`tool_choice: auto` 可用。改为遇到这种 400 自动用 auto 重发一次并按「地址 + 模型」记住；新增 1 项测试（重发一次、之后直接用 auto、不计失败）。真实后台流程：deepseek-flash 连接测试 0.7 s，3 个真实回合 2.9–3.5 s 全部成功，每回合约 1200–1800 输入 / 600–700 输出 Token（含思考）；deepseek-v4-pro 21–75 s，一次超过 30 s 超时，不适合实时对局。JSON 模式两者都可用。
- 顺带修正：只填域名（如 `https://api.deepseek.com`）时请求地址多出一个斜杠（`//chat/completions`、`//systemone`），这是原有问题，Jev / 本地来源同样受影响。
- `npm test` 89 项全部通过。
- 版本号 0.5.5。仅本地提交，未推送、未发版。

---

# 0.5.4 验证记录（OpenAI 兼容模型）

日期：2026-09-24。新增「OpenAI 兼容」模型来源与模型列表选择；战绩记录模型来源类型、模型、请求地址与 Token。

- `node --test test/*.test.mjs`：81 项通过；打包检查 1 项需先构建（本次按要求未构建项目 `dist/`），另在临时目录构建确认游戏页脚本不含任何模型地址、`fetch` 或授权头。新增 13 项：请求体（函数调用的候选枚举、JSON 模式）、战况裁剪、回答解析（工具调用 / 代码块 JSON / 包装对象 / Token 用量）、非法选择回退等待并标记、全部非法拒绝、模型列表解析、设置校验与字段报错、后台真实流程（请求地址与密钥、会话与战绩记录模型信息和 Token、日志带理由与输出 Token、不含密钥）、模型列表只限弹窗且不把已存密钥发往其他服务、错误提示（空列表 / HTTP 带服务原因 / 未授权）、连接测试与换服务要求新密钥、更长的超时与过期容忍。
- 界面预览（临时构建）：三个来源切换、填密钥后获取模型列表、下拉选择、切换调用方式、保存、测试连接成功；密钥错误只在密钥框下提示；中英文文案；数据面板战绩表 Token 列、总 Token、单场的模型 / 地址 / Token 行与带理由的逐条决策。
- 真实服务（用户提供的 OpenAI 格式中转）走完整后台流程：获取到 18 个模型；gpt-5.6 函数调用连接测试 2.8 s；用 0.4.3 那局日志里的 3 个真实回合决策，耗时 6–25 s，每回合约 800–1300 输入 Token、85–150 输出 Token；在 Laya 选「等待」的回合里，模型选择了探图、建哨戒炮、造防空履带车。JSON 模式（gpt-5.6-luna）也能解析，但 20–30 s，一次超时。服务不支持的模型返回的原因（如「该模型不支持」）原样显示在错误里。战绩与日志中不含密钥。
- 未在真实对局中托管验证。版本号升到 0.5.4。仅本地提交，未推送、未发版。

---

# 0.5.3 验证记录

日期：2026-09-24。修复数据面板无法读取数据。

- 现象：面板显示「此操作只允许在插件窗口中执行」，战绩列表为空，但对局结束的战报文件正常下载（说明数据已记录）。
- 原因：后台的可信判断要求「发消息者不在标签页里」，只有弹窗满足；面板通过新标签页打开，被当成不可信来源拒绝所有读取。
- 修复：改为「扩展 ID 一致 + 发消息者地址是扩展自己的页面 + 主框架」，弹窗与面板都通过；游戏页内容脚本、其他扩展、子框架、伪造地址仍被拒。新增 1 项测试覆盖以上各情形；npm test 75 项通过。
- 已记录的战绩无需重打，面板打开后直接可见。仅本地提交，未推送、未发版。

---

# 0.5.2 验证记录

日期：2026-09-24。分析用户两份战报（本地 Laya；工程师训练被问 103 次、91% 等待；策略里只有修桥没有占领）后：新增占领候选与跟进、按目标训练工程师、关键候选自动兜底、历史参考与老选项降级。

- `npm test`：74 项通过。新增 5 项：占领候选的取舍（中立优先、无防守敌方经济建筑次之、有守军 / 可驻守 / 防御不选）；无工程师时训练候选带 OBJECTIVE 且资金充裕才自动；占领命令下达、150 拍后空闲重发、建筑易手完成、已占有拒绝；连续两次拒绝占领后自动执行并锁定工程师；历史提示（连选 5 次损失 6 击毁 0 → 提示 + STALE，升级 2 级撤下、保卫基地不撤、顺利时不加）。
- 通用兜底替换了原探索专用兜底，探索测试保持通过（reason 仍为 auto_explore）。
- `tools/analyze-log.mjs` 新增自动兜底动作与带历史提示的决策计数。
- 未在真实对局验证；仅本地提交，未推送、未发版。

---

# 0.5.1 验证记录

日期：2026-09-24。跨机器访问：私有网络判定放宽，授权写法修正，服务端地址发现。

- 发现并修正：清单 `optional_host_permissions` 里 `http://10.*/*` 这类 IP 通配不是合法的 Chrome 匹配模式，从另一台机器填局域网地址时授权可能申请不到；改为 `http://*/*` + `https://*/*` 作为可选范围，实际按填写地址精确申请。打包检查新增：每条可选权限必须是合法匹配模式。
- 地址不由扩展预设：本机、局域网、Tailscale 等私有网络地址与所有 https 地址默认可用；其他 IP / 域名走明文 http 需先在「允许的外部地址」列表里允许（最多 32 条，添加时申请一次该地址访问权，可移除）。后台在保存、开始与每次请求时都校验，移除后正在进行的托管立即拒绝该地址。`npm test`：68 项通过。用例：Tailscale 100.x、MagicDNS、单段主机名、`.lan` / `.home.arpa`、169.254、IPv6 私有地址、https 公网均不提醒；`http://example.com`、8.8.8.8、100.128.0.1、公网 IPv6、自建域名明文 http 均接受但提醒；ftp、带密码、带查询参数的地址拒绝。
- 本地服务重启后横幅列出本机全部地址并标注类型（127 loopback、10.0.25.215 LAN、100.118.47.84 Tailscale / CGNAT）。
- 仅本地提交，未推送、未发版。

---

# 0.5.0 验证记录

日期：2026-09-24。对局历史保存、独立数据面板、对局元信息采集，弹窗职责收窄。

- `npm test`：68 项通过。新增：页面元信息进入战绩与该场日志且不带多余字段、每场日志单独存储并可读取、标签 / 备注 / 结果更新与校验、删除一场同时删日志、清空全部同时清日志、超过 100 场时最旧记录连同日志淘汰；打包检查确认 dashboard 三个文件存在、弹窗不再含战绩与日志面板但含数据面板入口。
- 预览页加载真实 dashboard.js：总览条、战绩表两行、点开详情显示元信息（页面、地址、地图尺寸、开始拍、玩家列表含 AI / 人类与阵营）、四张曲线、各组等待率、关键事件 5 条、决策记录 60 行、保存标签后标题与表格同步更新、全局日志汇总。
- 元信息来源确认：公开 API 只有 players / me / map.size / tick / time，没有地图或任务名称；以页面标题与地址作为对局标识。
- 顺带修：曲线采样原先只保留最近 300 个点（2 秒一个，只覆盖最后约 10 分钟）；改为缓冲满后隔点抽稀并把采样间隔翻倍，整场都有曲线；图表断线阈值按实际采样间隔自适应。测试确认 1400 个样本抽稀后覆盖首尾、点数在 150–300 之间。
- 仅本地提交，未推送、未发版。

---

# 0.4.7 验证记录

日期：2026-09-24。经济目标改为按收入、资金趋势与矿车闲置动态决定。

- `npm test`：66 项通过。新增经济 4 项：资金 5 万且上升时目标 1 辆矿车、不建第二精炼厂、战斗单位不再被「2 辆矿车」门槛挡住；资金 900 且下降时目标 2 辆并给出矿车候选；两辆矿车找不到矿闲置时目标停在 2；收入按「资金变化 + 已花费」计算，无精炼厂时目标 0 且先建精炼厂。既有策略测试中「两矿车基地应扩矿」场景改为两座精炼厂、矿车在工作，其余断言保持。
- 未在真实对局验证；仅本地提交，未推送、未发版。

---

# 0.4.6 验证记录

日期：2026-09-24。悬浮窗战损、自动战报、按态势升级打法。

- `npm test`：62 项通过。新增：悬浮状态接口返回双方损失、托管结束自动下载战报（文件名 jev-reports/jev-report-<时间>.json、内容含战绩 + 该场日志 + 统计、不含密钥、关闭后不下载、设置只允许弹窗改）、交换比失利时升级（放弃当前进攻、门槛 8 → 12、生产目标 +4、说明含「ATTACKS ARE FAILING」与前沿驻守提示、交换比转好后回落）、进攻停滞无损失也升级并把目标旁空民房作为前沿驻守候选。
- 预览页（加载真实 content.js）确认悬浮窗新增四个损失数字与配色；设置页新增自动战报开关默认勾选。
- 未在真实对局验证自动战报下载与升级打法的实际效果；仅本地提交，未推送、未发版。

---

# 0.4.5 验证记录

日期：2026-09-24。保存设置改为先保存再申请站点访问权限。

- `npm test`：59 项通过。新增：未授权时保存成功且令牌已存储、开启托管与测试连接提示「授权访问」、授权后无需重填即可开始。
- 预览页（模拟拒绝授权）确认：保存后提示「设置已保存，但还没有授权访问…」，输入框里的令牌仍在，顶部出现「授权访问」横幅；点击后横幅消失并提示已授权。
- 本次仅本地提交，未推送、未发版。

---

# 0.4.4 验证记录

日期：2026-09-24。分析用户第二份日志（本地 Laya，506 次决策，对局以 battle_ended 结束）后：探图 205 次被问、203 次选等待，海军 100 次被问多选巡逻，结束时来不及读胜负。

- `npm test`：58 项通过。新增：战损账本（首次观测只登记不计数、新单位计新建、视野内消失的敌人计击毁、迷雾中消失不计并在超时后遗忘）、连续三次拒绝探图后自动出发一次、能打到威胁的舰船编入保卫任务、未战败结束标记为 ended、结果标记只允许弹窗且校验取值、战绩曲线带兵力与战损字段。
- 预览页确认「趋势」与战绩详情各新增两张曲线（每张 3 条线），结果标记按钮可切换、列表同步更新并提示，清除后恢复。
- 未在真实对局验证自动探索与海军追击，需要用户实测。

---

# 0.4.3 验证记录

日期：2026-09-24。分析用户导出的战役日志（本地 Laya，386 次决策）后修三处：后台标签页定时器节流导致决策每分钟一次（日志中 tick 21692–64892 期间决策严格每 3600 tick 一次）、海军不自卫、缺少对局总结。

- `npm test`：54 项通过。新增：游戏拍子唤醒决策循环（定时器失效时每个新拍子恰好问一次、同拍不重复、停止后注销回调且不再提问）、无 `onTick` 时退回定时器并在开始事件中标注、潜艇在射程内自动回击且不被通道清理移动、战绩记录（计数、结果、资金曲线、按时间倒序、重复停止不重复记录、监视不产生记录、导出不含密钥、清空）。
- 预览页确认「战绩」标签：列表（时间、胜负、时长、来源、回答数、结束资金）、详情三项指标、事实行、经济与决策曲线、导出与清空按钮。
- 未在真实对局验证后台节流修复；需用户切走标签页后观察决策计数是否持续增长。

---

# 0.4.2 验证记录

日期：2026-09-24。出击门槛改为按实际生产能力与部队增长判断；探图不再因见过敌方建筑而停止；新增「本局目标」设置。

- `npm test`：50 项通过。新增战役场景 4 项：有车厂时仍按 8 车 + 防空规则（7 车无护卫不出击、补齐后出击，期间探图继续）；无车厂时 8 名武装步兵即出击（留 1 名侦察兵）、1 名时继续训练并侦察；完全不能生产时有 2 个单位就出击；部队 2700 tick 未增长即出击、刚增长则重置计时；见过敌方哨塔后探图仍提供；目标文本进入战术说明与 state.objective。后台 1 项：目标保存时压缩空白、超 300 字拒绝、开启托管时传给页面、内容脚本拿不到。
- 既有遭遇战测试全部保持通过（含「基地防空不算随军护卫」「8 车才进攻」等）。
- 预览页确认设置页新增目标输入框、超长时行内提示。
- 未在真实战役中验证效果；需要用户实测并导出决策日志。

---

# 0.4.1 验证记录

日期：2026-09-24。新增决策日志（扩展本机记录 + 导出 + 汇总）、本地服务请求日志和离线分析脚本。

- `npm test`：45 项通过。新增日志模块 4 项（记录裁剪与脱敏、事件归类、条数 / 体积上限、统计口径）和后台 1 项（问答 / 动作 / 失败入库、导出仅限弹窗、导出不含密钥与令牌、后台重启后仍可读、清空）。
- 实际排查：本地 Laya 服务日志显示扩展已发来 551 次请求全部 200，确认托管走的是本地模型而非官方 Jev。用真实规模请求测得 Laya 在 construction / defenses / vehicles / infantry 组几乎总选 wait（0.72–0.99），只有 tactics 组选进攻，这是「有回答但不造东西」的直接原因。
- 预览页确认：设置页底部显示日志汇总（记录数、决策数、失败数、平均响应、动作受理 / 等待 / 未执行、各组等待率）、导出触发下载并提示文件名、清空后显示空状态；保存过「本地 Laya」后重新打开设置页直接选中本地来源。
- 第二个本地服务实例加 `--log` 跑真实规模请求，JSON Lines 每行含候选、选择、概率、耗时；`tools/analyze-log.mjs` 对该文件和导出 JSON 都能输出汇总。

---

# 0.4.0 验证记录

日期：2026-09-23。新增「模型来源」开关（Jev 云端 / 本地 Laya）和本地 Laya 决策服务；未修改游戏引擎、玩家 API 或策略。

## 自动检查与发布包

- `npm test`：39 项通过，0 失败。新增 3 项：两种来源各自保存地址与密钥、本地来源无需密钥且不发送 Authorization 头、切换来源或修改当前来源配置停止托管、清除本地令牌不影响 Jev 密钥、连接测试跟随当前来源、Jev 来源仍要求密钥、本地服务 HTTP 503 记为失败但不停止托管、局域网私有地址允许 HTTP 而公网地址仍拒绝。
- `dist/page.js` 不再包含任何模型服务地址（默认值改为可摇树的纯对象），打包检查同时校验 `api.typesafe.ai` 与 `127.0.0.1:8742` 都不出现在游戏页脚本中。
- `npm run package` 成功。包路径：`artifacts/werhd-jev-extension-0.4.0.zip`。SHA-256：`23926519255a090f6b1befa421fed4943ce4d24f5f187097f25f58713c229158`。

## 本地 Laya 服务

- `npm run laya` 用 laya-vs-jev 的 Python 3.14 / MLX 环境加载 `laya-multilingual-mlx`（0.8–1.1 秒），监听 `127.0.0.1:8742`。
- 用扩展「测试模型连接」的原样请求体调用 `/v1/systemone`，返回 `choice: ok`，26 ms。用 5 组候选、7 KB 战况的真实规模请求连续 5 次，平均往返 181 ms，5 个回答全部通过扩展的候选校验（`validateAnswer`）；60 个候选的极端请求仍返回 200。错误路径：错误路径返回 404、缺少 questions 返回 422、非 JSON 返回 422，服务不中断。
- 用假模型对服务单元检查：状态字段重排只改顺序不丢字段；缺 state、空 questions、非法类型均被拒；设置令牌后无令牌或错误令牌返回 401，正确令牌返回 200；超过 256 KB 的请求体返回 413 且不触达模型；`/health` 报告令牌开关与请求计数。
- `--lan` 模式监听 `0.0.0.0`，启动时打印 `http://10.0.25.215:8742/v1` 与访问令牌，令牌持久保存在 `~/.laya-server-token`（0600）。从局域网地址无令牌调用返回 401，带令牌调用返回正确回答。

## 界面

- 密钥与本地令牌输入框右侧新增「显示 / 隐藏」按钮，切换后输入框在明文 / 密文之间变化，标题随语言切换（显示密钥 / Show key）。
- 「测试模型连接」按钮自身显示三种状态：测试中（禁用 + 转圈 + 「正在测试 Jev 连接…」）、成功（绿色「✓ Jev 连接成功 · 123 ms · jev-1.13」）、失败（琥珀色「✗ Jev 连接失败，…」）；结果不再写入上方提示条。修改任一设置或切换来源后按钮复位为「测试模型连接」；未保存时点击仍提示先保存。以上在预览页中逐一点击并截图确认。
- 地址、密钥、模型名在保存时和后台读取时都去掉首尾空格，新增测试覆盖带空格输入。
- 打开设置时已保存的 JEV 密钥 / 本地令牌直接填入输入框，默认以密码圆点遮住，点小眼睛才明文；保存后重新填入并恢复遮住。表单里的内容就是保存的内容：清空本地令牌并保存即清除；Jev 来源清空密钥保存会行内提示。更换 Jev 服务地址时若密钥未变会拒绝保存并提示重新输入。密钥只发给扩展自己的弹窗，内容脚本、状态接口、悬浮窗仍拿不到（测试覆盖）。预览页确认：加载即显示遮住的密钥、小眼睛切换明文、改密钥保存后回填并遮住。
- 表单校验改为行内提示：保存或测试时，每个有问题的输入框红框并在框下方显示原因（密钥为空、地址非法、模型名非法、快捷键非法、上限超范围），一次显示全部问题，焦点落到第一个；「更多设置」里的字段出错会自动展开。修改该字段后提示立即消失，切换来源清空全部。后台返回的密钥 / 地址类错误也映射到对应输入框，其余错误仍走顶部提示条。预览页用空密钥 + 公网 HTTP 地址 + 空上限三处同时出错验证，截图确认样式与中英文文案。
- `node tools/preview.mjs` 预览：设置页顶部显示「模型来源」分段开关，切到「本地 Laya」后只显示本地服务地址、访问令牌（可选）和启动提示，Jev 密钥 / API 地址 / .env 导入隐藏；「更多设置」按来源切换 Jev 模型名 / 本地模型名；按钮文案改为「测试模型连接」。中英文均已切换检查。

## 验证边界

- 本次没有在真实 Chrome 中加载 0.4.0 扩展并开局：自动化环境无法加载未打包扩展（品牌版 Chrome 已移除 `--load-extension`，Chrome for Testing 在本会话中启动卡住）。真实浏览器中的保存授权、测试连接和对局托管由用户手动验证。
- 本地模型受 1024 token 上下文限制，战况被截断，决策质量未评估，不代表胜率。

---

# 0.3.0 验证记录

日期：2026-09-23。仅修改独立扩展，未修改游戏引擎、公开玩家 API 或线上站点。

## 自动检查与发布包

- `npm test`：36 项通过，0 失败。新增 7 项覆盖展示条件、停止 / 隐藏后的迟到响应、标签页可见性、连接失效、拖动边界、文档隔离、设置持久化与状态通知。
- 展示条件为 `running && showOverlay`。停止、设置关闭、页面隐藏或销毁后，正在等待的旧状态响应不能重新挂载窗口。重新允许展示必须先确认当前运行状态。
- 新增内容脚本状态接口只返回同一标签页、文档和来源的显示字段；不返回密钥、会话令牌、原始事件或 API 地址。读取期间只检查现有控制器状态，不注入玩家、不调用模型、不提交游戏指令。
- 设置开关即时保存，后台重启后保留；切换开关不会创建或停止托管会话。模型调用、游戏命令与既有策略测试保持通过。
- `npm run package` 成功；ZIP 的 14 个条目完整性校验通过。包路径：`artifacts/werhd-jev-extension-0.3.0.zip`。SHA-256：`8eacea5a6d7e7c29632ef3a51f37a8bd6163da5ae46d4f026e24777549c418b6`。

## 真实 Chrome / 线上游戏验证

- 已安装扩展更新到 0.3.0，ID 不变，保留原有密钥。测试地址为 `https://ra2web.github.io/`，游戏版本 0.87.0；正常菜单进入「湖泊之战 (2)」，利比亚，对手 AI-简单，资金 10000、初始部队 0、速度 6。
- 主菜单和进入对局后、未开启托管时，均无悬浮状态窗。按 `Alt+Shift+J` 后出现中文状态窗，初始 tick 518、资金 10000、回答 0；随后读取真实回答 2、资金 9569、响应 337 ms。
- 点击收起后仅保留标题状态条。在收起按钮有焦点时再按同一快捷键，出现停止提示，状态窗立即从页面移除。弹窗确认该会话回答数 3、停止按钮禁用。
- 在设置中关闭展示并切换英文，关闭后重新打开弹窗仍为未勾选。此时通过工具栏按钮开启托管，游戏页无状态窗；弹窗确认托管运行、回答 1、资金 6447，说明隐藏展示没有阻止托管。
- 同一运行会话重新允许展示，立即出现英文状态条；展开后显示 `JEV · wannafire`、`Jev is in control` 及真实指标。标题拖动后截图确认窗口从左侧移至画面内侧。
- 使用工具栏停止按钮停止第二段托管，回答数 37；恢复中文后关闭弹窗，页面没有状态窗。随后快捷键重新开启，正常使用「放弃任务 → 退出」离开对局；结算时状态窗自动移除，扩展显示「对局已结束」、回答 2、tick 8611，停止按钮禁用。
- 收尾停留中文、允许展示开启、托管停止。关闭了本次临时测试标签页，保留用户原有标签页。

## 验证边界

- 本次验证悬浮状态窗及控制状态同步，不评估策略胜率。测试期间多次手动开启 / 停止，最后主动退出产生失败结算，不作为完整策略对战结果。
- 自然战败、HTTP 错误和请求预算停止沿用既有后台停止流程；自动测试覆盖停止通知，新增窗口实测覆盖手动快捷键、停止按钮和主动退出。全屏、Edge、窄屏及长期挂机未做本轮人工验收。

---

# 0.2.1 官网入口

日期：2026-09-23。弹窗底部与使用说明新增随语言切换的官网入口：中文「王二火大」→ https://wangerhuoda.com/；英文「wannafire」→ https://wannafire.com/。使用新标签页打开。

- `npm test`：既有 29 项通过；构建、打包与 ZIP 完整性检查通过。
- 已将原 Chrome 扩展重新加载为 0.2.1。在真实扩展弹窗及帮助页分别切换中文/英文，核对显示名称与链接目标同步变化。
- 收尾恢复中文，未开启托管。本次仅新增界面链接，没有修改游戏 API 或策略；没有重新开展对局测试。

---

# 0.2.0 验证记录

日期：2026-09-23。只修改 `werhd-jev-extension`，没有修改游戏、游戏 API 或线上站点。

## 自动化与界面

- `npm test`：29 项通过，0 失败。保留 0.1.1 的 22 项回归；新增只读观测不产生模型请求/命令、跨后台重启保存采样、识别新局、语言切换保留凭据及运行状态、零值/未知值/可见样本边界、300 点容量与采样间隔、时钟回退、空图/负值/常量曲线、中英文文案完整性。
- 内置本地预览以 410×600 弹窗尺寸检查中英文战场、趋势和设置。两张折线图共 3 条曲线、鼠标定位采样、资金双线和图例可见；DOM 测得可用宽度与滚动宽度均为 395px（扣除滚动条），无横向溢出。空对局有明确等待采样状态。预览数据只用于排版验证，不作为实战证据。
- 中文/English 持久化切换，英文名称为 WannaFire；帮助页支持两种语言。单位名称使用游戏提供的标签，英文界面使用规则代号。

## 已安装 Chrome 与线上实战

- 在原扩展 ID `gdlbfdhmjlbgokjjnocibejeaagnphpi` 上重新加载 `dist/`，Chrome 显示 0.2.0。权限范围不变。已保存密钥、API `https://api.typesafe.ai/v1`、快捷键 Alt+Shift+J 和镜头选项均保留。
- 主菜单打开弹窗不自动托管；中英文切换立即生效。关闭再打开弹窗仍为英文。
- 从 `https://ra2web.github.io/` 正常菜单进入「城市突击战 (2)」，己方美国、对手 AI-简单随机、资金 10000、初始部队 0、速度 6。
- **未开启托管时**，弹窗 tick 718、资金 10000、模型回答 0、响应耗时为空，态势为 LIVE / read-only，读到自身基地位置、电力和单位状态。只读监控调用模型次数为零的机制由后台自动化测试覆盖。
- 17:18 左右手动开启，真实 Jev 返回有效回答：回答数由 0 到 2、3、4；响应时间观察到 1132、362、298ms。累计回答曲线和真实资金/队列剩余投入双线随战况更新。过程中切换中文，计数保持 3、托管仍在运行。
- tick 3300 点击停止时累计 4 次模型回答、资金 6783。随后 tick 3868、4318、4678 均维持 4 次回答，同时资金和实时态势继续刷新；这是只读观察和已有生产继续运行，不是重新托管。
- 停止后真实态势读到视野内敌军 1、基地附近敌军 1、采矿单位 1、电力 200/50、矿场队列 100% 待放置。模型控制已停止，监控没有代为放置建筑。
- 通过游戏正常菜单「放弃任务 → 退出」结束本次短局。重新打开弹窗显示「最后观测 · 非实时」，保留 tick 5308、4 次回答、资金 6450 的最后快照；没有自动继续控制。

- 发布包 `artifacts/werhd-jev-extension-0.2.0.zip` 的 14 个条目完整性通过，实际本地密钥扫描无命中，不含开发预览、测试、依赖或配置文件。SHA-256：`dda756c541d3f62cf7d6af9d65c995c7d9a076ea0930fb19e37eec3f82ad04dc`。

- 收尾重新加载最终打包版本并刷新游戏页，默认停留中文、未托管状态；本地模拟预览已关闭。
- 三个游戏 API / 镜头文件 SHA-256 与 0.1.1 验证记录中的基线一致。

## 边界

- 本次新增界面和数据链路已用真实 Chrome / 线上游戏 / Jev 验证。射程告警布局使用模拟压力样本验证；其计算沿用已有射程分析回归，短实测没有等到光棱压制场景。
- 图表是当前会话的有界采样，不是跨浏览器重启保存的完整战报；每次开启托管重置计数，新局清空旧图。资金、电力和血量未知值不伪装成零，结束后的快照标明非实时。
- 不属于胜率或策略强度验收。其他地图、阵营、Edge 和长期运行仍未完成验证。

---

# 0.1.1 验证记录

日期：2026-09-23。本项目只实现独立浏览器扩展；本次未改动游戏引擎、玩家 API 或线上站点。

## 已完成

- `npm test`：22 项通过，0 失败。包含既有的策略、射程防守、部署、兵种和通道、特殊任务、镜头测试，以及执行器的过期战况、停止、自然结束保护。
- 扩展后台测试：使用模拟 Chrome API 和模拟 Jev 响应，验证密钥仅供可信扩展页面管理、请求地址来自设置、候选校验、标签页/文档/会话隔离、停止后拒绝迟到回答、并发保护、后台重启不重置预算、预算耗尽停止、HTTP 402 立即停止、更换凭据停止旧会话、页面导航撤销会话。
- 页面消息测试：实际页面适配器与编译后的 content script 在隔离测试上下文中收发消息；游戏策略替换为测试桩。验证请求/响应、取消、重复注入、会话替换和对局身份切换。这不等同于 Chrome 真实执行上下文验证。
- 界面人工操作：在浏览器以 390×600 尺寸打开 `tools/preview.mjs` 的模拟预览，验证密钥字段、保存、录入 Ctrl+Shift+K、开启/停止后的按钮与状态切换、纵向滚动；修复了窄窗口横向滚动。预览不连接模型或真实游戏。
- `npm run package` 成功；`unzip -t artifacts/werhd-jev-extension-0.1.1.zip` 全部条目校验通过。包内仅包含编译脚本、弹窗/帮助页、manifest 和图标，不含测试桩、源码依赖、代理服务或任何密钥。
- 游戏 API 文件的 SHA-256 与本次工作开始时一致：`PlayerConsoleApi.ts`、`PlayerConsoleTypes.ts`、`PlayerConsoleCamera.ts`。

## Chrome 与真实 Jev 已观察到的结果

- 用户确认安装后，通过 Chrome「加载未打包的扩展程序」加载 `dist/`，扩展 ID 为 `gdlbfdhmjlbgokjjnocibejeaagnphpi`，页面显示扩展已加载且启用；真实工具栏弹窗正常连接后台。
- 通过扩展新增的本地文件导入入口，读取此前 `.env.local` 中的三个 Jev 配置字段，并在弹窗保存。密钥没有输出到对话或日志；其他配置未导入。
- 真实「测试 Jev 连接」请求成功，弹窗显示 `937 ms`。本次服务已返回有效候选回答，不沿用历史 HTTP 402 作为当前结论。
- 在游戏主菜单点击开启，明确提示「请先进入一场正在运行的对局」，没有开始游戏控制。
- 在线地址为 `https://ra2web.github.io/`，游戏界面版本 `0.87.0`；通过正常菜单开始「城市突击战 (2)」，己方美国，对手 AI-简单、随机阵营，资金 10000、初始部队 0、速度 6。
- 16:48:24 开启后，日志出现 `deploy_base`、`GAPOWR` 和建筑放置；游戏画面确认移动基地变成建造场、电厂实际出现，资金由 10000 减少。
- 16:49 左右，模型计数先为 7，随后到 15；显示真实响应 363–596 ms、前沿侦察和兵种生产。后续日志显示 `defend_base`、多个单位的「基地接敌」命令。
- 首次停止显示停止状态、15 次决策；随后发现另一次会话重新开启且计数重置。电脑操作工具两次提示用户正在操作同一 Chrome 窗口，因此这段受干扰的样本不计入停止稳定性通过证据。

## 闭环完成结果

1. 第一局自然战败：游戏结算显示「失败」，地图「城市突击战 (2)」，游戏时间 `0:14:13`。己方美国：摧毁 3、损失 25、建造 13、分数 55；对手伊拉克 AI-简单：摧毁 19、损失 3、建造 34、分数 515。扩展先自动停止，记录 `outcome` 和 `defeated_or_observer`，最终会话计数 19。这局有开局后延迟接管和中途开关操作，不能作为公平的策略强度对比。
2. 用户交还 Chrome 后，按同样设置开第二局短流程测试。旧会话未自动接管新局；手动按 `Alt+Shift+J` 出现「Jev 托管已开启」，弹窗显示新对局 tick 691、2 次模型决策和真实资金。
3. 点击「停止」后：tick 1136、决策 3、资金快照 8938、响应 938 ms。等待 12 秒（超过 8 秒后台请求超时），重新完整读取弹窗，仍为相同计数和快照，「停止」按钮禁用，无自动重新开启。
4. 关闭弹窗后，`Alt+Shift+J` 分别验证重新开启和停止，游戏页提示与动作一致。
5. 再次开启后，通过游戏菜单「放弃任务 → 退出」结束短测试。扩展自动显示「对局已结束」，停止按钮禁用。本段会话计数 0，没有把退出后回答计入有效决策。
6. 实际安装、配置保存、直连 Jev、MAIN/ISOLATED 消息桥、正常玩家 API 命令效果、关闭弹窗继续运行、手动停止、快捷键、自然战败和退出清理形成闭环。没有修改游戏代码或线上 API。
7. 收尾已在 Chrome 重新加载 `0.1.1`。真实弹窗显示该版本、已保存密钥、默认 API 和快捷键均保留；刷新后的游戏页处于未托管状态，没有自动发起新会话。

## 尚未完成

- 未验证其他阵营/地图组合、困难 AI、长期挂机稳定性或获胜能力。本次接入链路闭环通过，不是策略强度/胜率验收。
- Edge、其他操作系统及浏览器重启后的人工验证。

## 用户安装后验收顺序

1. 扩展管理页加载 `dist/`，确认无加载错误；在弹窗保存密钥和默认 API 地址。
2. 打开在线游戏并正常进入对局。首次真实请求若返回 401/402/403，应看到明确状态并停止，检查服务/账户后再手动开启。
3. 开启后确认决策计数和战况更新，并观察实际建造、单位命令及可选镜头移动。
4. 按已配置快捷键停止，确认新的模型请求与命令停止；再次开启可继续当前局。
5. 退出或结束对局，确认不会自动接管下一局。

尚未完成的项目不得表述为已经通过。
