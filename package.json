{
  "name": "werhd-jev-extension",
  "version": "0.7.5",
  "private": true,
  "type": "module",
  "description": "王二火大 Jev 浏览器托管扩展",
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "npm run build && node --test test/*.test.mjs",
    "package": "npm run build && node tools/package.mjs",
    "laya": "sh tools/laya-server.sh"
  },
  "devDependencies": {
    "esbuild": "0.27.7"
  },
  "engines": {
    "node": ">=20"
  }
}
